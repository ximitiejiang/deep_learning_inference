//
// Created by ubuntu on 11/12/19.
//

#ifndef TEAT_VECTOR_OP_H
#define TEAT_VECTOR_OP_H


// 移除数组中多余的元素(LC-26): 并返回最终的数组长度，空间复杂度O(1)即不能使用额外存储空间
// 例如：nums = [0,0,1,1,1,2,2,3,3,4]，返回5，数组变为[0,1,2,3,4]
int removeDuplicates(std::vector<int> &nums){
    int length = 0;


    return length;
}


// 寻找数组中和为0三个数(LC-15): 三个数不重复，返回可能的组合。
// 例如：nums = [-1, 0, 1, 2, -1, -4]，返回[[-1, 0, 1],[-1, -1, 2]]
std::vector<std::vector<int>> threeSum(std::vector<int> &nums){

}
#endif //TEAT_VECTOR_OP_H